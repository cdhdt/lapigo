entities:
  article:
    fields:
      id: { type: uuid, pk: true }
      status: { type: enum, values: [draft, published], required: true }
    filters: [status]
    indexes:
      - filters: [status]
