entities:
  article:
    fields:
      id: { type: uuid, pk: true }
      author: { type: belongsTo, target: user, required: true, on_delete: set_null }
  user:
    fields:
      id: { type: uuid, pk: true }
